package com.example.iafinal;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.fxml.Initializable;
import javafx.scene.Node;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.TextField;

import javafx.scene.control.TableColumn;
import javafx.scene.control.TableView;
import javafx.scene.control.cell.PropertyValueFactory;
import javafx.stage.Stage;

import java.io.IOException;
import java.net.URL;
import java.util.ResourceBundle;

public class HelloController{

    /*


    //these are all for switching scences
    private Stage stage;
    private Scene scene;

    private Parent parent;

    //this should be for the teamView class---> change later

    @FXML private TableColumn<Player, String> nameTableView;
    @FXML private TableColumn<Player, String> positionTableView;
    @FXML private TableColumn<Player, String> numberTableView;

    //this is for the player class
    // to do: need to be able to show screen addTeam


    //change this also into the viewTeam thingy
    @Override
    public void initialize(URL location, ResourceBundle resources){
        nameTableView.setCellValueFactory(new PropertyValueFactory<>("name"));
        positionTableView.setCellValueFactory(new PropertyValueFactory<>("position"));
        numberTableView.setCellValueFactory(new PropertyValueFactory<>("number"));

    }

    //this method switches from scene addTeam to createTeam
    public void SwitchToSceneCreateTeam (ActionEvent event) throws IOException{
        Parent parent = FXMLLoader.load(getClass().getResource("createTeam.fxml"));
        stage = (Stage)((Node)event.getSource()).getScene().getWindow();
        scene = new Scene(parent);
        stage.setScene(scene);
        stage.show();
    }

    //this method switches from scene createTeam to TableView
    public void SwitchToSceneTableView (ActionEvent event) throws IOException{
        Parent parent = FXMLLoader.load(getClass().getResource("TableView.fxml"));
        stage = (Stage)((Node)event.getSource()).getScene().getWindow();
        scene = new Scene(parent);
        stage.setScene(scene);
        stage.show();
    }


    //adds players into the team when the button "save" is clicked


    //method to see the table
    public void TeamView(ActionEvent actionEvent){

    }

     */



}


